[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "orchestrus"
version = "0.1.0"

[tool.poetry.dependencies]
python = "^3.10"
firecracker = "^0.12.0"